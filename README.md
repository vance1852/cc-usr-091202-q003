# 城市道路报修 Node.js 工作区

这里保存道路报修的交换样例和 Node.js 启动骨架。`fixtures/context.json` 提供道路网格，`fixtures/hotline.csv` 保留脱敏热线记录，后续功能应围绕这些资料提供可复核的业务入口。

运行 `npm start` 可检查程序入口，运行 `npm test` 可执行当前测试目录。项目代码使用 Node.js 内置能力，数据文件不包含真实个人信息。
